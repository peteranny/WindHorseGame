import React from "react";
import styles from "./styles.css";

// The connector drawn between two adjacent members of a linked (same-
// family) attack group.
export const AttackWire = () => (
  <span className={styles.attackWire} aria-hidden="true" />
);
