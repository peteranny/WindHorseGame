// Stands in for any binary asset import (png/jpg/jpeg/svg) under Jest -
// react-test-renderer never actually paints these, so any fixed string
// works as the `src` a component reads.
module.exports = "test-file-stub";
